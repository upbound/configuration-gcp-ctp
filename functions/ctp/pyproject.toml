[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "function"
description = "GCP GKE Control Plane composition function."
readme = "README.md"
requires-python = ">=3.11,<3.14"
license = "Apache-2.0"
dependencies = [
  "crossplane-function-sdk-python==0.11.0",
  "click==8.3.2",
  "grpcio>=1.76.0",
]
dynamic = ["version"]

[project.scripts]
function = "function.main:cli"

[tool.hatch.build.targets.wheel]
packages = ["function"]

[tool.hatch.version]
path = "function/__version__.py"
validate-bump = false

[tool.hatch.metadata]
allow-direct-references = true
